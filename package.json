{
  "name": "simplecrudjs",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 3030",
    "build": "vite build",
    "preview": "vite preview",
    "server": "json-server server/db.json --port 3001"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.3",
    "vite": "^6.0.5"
  }
}
